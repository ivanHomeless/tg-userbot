# 🤖 Telegram AI-Content Reposter (Userbot)

Умный юзербот для автоматического копирования контента из выбранных Telegram-каналов в твой собственный с использованием ИИ (OpenRouter) для уникализации текста.

## ✨ Основные возможности

* **AI-Рерайт**: Каждый пост проходит через LLM (Gemini, GPT, Claude) для изменения стиля и уникализации.
* **Поддержка альбомов**: Корректно собирает медиа-группы (фото/видео) и отправляет их одним постом.
* **Отказоустойчивость**: 
    - Автоматическая ротация API-ключей OpenRouter при достижении лимитов.
    - Механизм повторных попыток (retries) при сбоях сети.
    - Авторестарт службы через `systemd`.
* **Умные лимиты**: Соблюдение пауз между постами и обход ограничений Telegram на длину подписи (1024 символа).
* **База данных**: Исключает повторную публикацию одних и тех же постов.

## 🛠 Технологии

- **Python 3.10+**
- **Telethon** (Telegram API)
- **OpenAI SDK** (для связи с OpenRouter)
- **SQLite** (хранение истории постов)
- **Systemd** (управление процессом в Linux)

## 📂 Структура проекта

```text
├── app/
│   ├── ai.py            # Логика работы с ИИ и ротация ключей
│   ├── bot_logic.py     # Обработка событий Telegram и сборка альбомов
│   ├── config.py        # Загрузка настроек из .env
│   ├── database.py      # Работа с БД (анти-дубль)
│   └── prompts.py       # Инструкции (System Prompt) для ИИ
├── data/                # Сессия Telegram и база данных SQLite
├── logs/                # Логи работы бота
├── tmp_media/           # Временная папка для загрузки медиа
├── .env                 # Конфиденциальные настройки
├── main.py              # Точка входа и инициализация
└── requirements.txt     # Зависимости
```

## 🚀 Установка и запуск

### 1. Подготовка

Склонируйте репозиторий и создайте виртуальное окружение:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройка окружения

Создайте файл `.env` и заполните его данными:

```env
# Telegram API (my.telegram.org)
API_ID=123456
API_HASH=your_hash_here
PHONE=+79991234567

# Настройки каналов
SOURCES=@channel_1,https://t.me/channel_2
DEST=@your_channel_username

# AI (OpenRouter)
# Можно указать несколько ключей через запятую для ротации
OPENROUTER_API_KEY=sk-or-v1-key1,sk-or-v1-key2
MODEL=google/gemini-2.0-flash-001
```

### 3. Первый запуск

При первом запуске нужно будет ввести код подтверждения из Telegram для создания файла сессии:

```bash
python3 main.py
```

## ⚙️ Автоматизация (Systemd)

Для работы в фоновом режиме на сервере создайте службу:

```bash
sudo nano /etc/systemd/system/tg_bot.service
```

```ini
[Unit]
Description=Telegram AI Userbot
After=network.target

[Service]
Type=simple
User=ваше_имя_пользователя
WorkingDirectory=/path/to/project
ExecStart=/path/to/project/venv/bin/python main.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

Управление службой:

```bash
sudo systemctl enable tg_bot   # Включить автозапуск
sudo systemctl start tg_bot    # Запустить сейчас
sudo systemctl status tg_bot   # Проверить статус
```

## 📝 Логирование

Все события записываются в `logs/bot_work.log`. Вы можете следить за работой бота в реальном времени:

```bash
tail -f logs/bot_work.log
```

## ⚠️ Важные замечания

**Лимиты**: Не ставьте слишком маленькую задержку `POST_DELAY`, чтобы избежать Flood Wait от Telegram.

**Медиа**: Папка `tmp_media` очищается автоматически при каждом запуске бота.

## 📦 Зависимости

Создайте файл `requirements.txt`:

```txt
telethon
python-dotenv
openai
```

## 🔧 Настройка

**Пути**: Не забудьте заменить `/path/to/project` в секции про Systemd на реальный путь к вашему боту на сервере.