# 🛑 Корректное завершение работы бота

## Обработка сигналов остановки

Бот теперь корректно обрабатывает сигналы завершения:

- **Ctrl+C** (SIGINT) — остановка пользователем
- **kill** (SIGTERM) — системная остановка
- **systemctl stop** — остановка через systemd

---

## 🎯 Что происходит при остановке?

### 1. Получение сигнала

```
Пользователь нажимает Ctrl+C
↓
⚠️  Получен сигнал SIGINT, завершаем работу...
```

### 2. Graceful Shutdown (корректное завершение)

```
👋 Останавливаем бота...
↓
🛑 Остановка rewriter...
🛑 Остановка awaiting_closer...
🛑 Остановка post_builder...
🛑 Остановка publisher...
↓
✅ Все задачи завершены
✅ Бот корректно остановлен
🛑 Завершение работы бота
```

---

## 📝 Пример логов

### До изменений (плохо):

```
^C
Traceback (most recent call last):
  File "main.py", line 25, in <module>
    asyncio.run(main())
  File ".../asyncio/runners.py", line 44, in run
    return loop.run_until_complete(main)
KeyboardInterrupt
```

### После изменений (хорошо):

```
^C
⚠️  Получен сигнал SIGINT, завершаем работу...
👋 Останавливаем бота...
🛑 Остановка rewriter...
🛑 Остановка awaiting_closer...
🛑 Остановка post_builder...
🛑 Остановка publisher...
✅ Все задачи завершены
✅ Бот корректно остановлен
🛑 Завершение работы бота
👋 Бот остановлен
```

---

## 🔧 Технические детали

### Signal Handler

```python
def signal_handler(sig, frame):
    """Обработчик сигналов (Ctrl+C, SIGTERM)"""
    signal_name = signal.Signals(sig).name
    logger.info(f"\n⚠️  Получен сигнал {signal_name}, завершаем работу...")
    shutdown_event.set()
```

**Зарегистрированные сигналы:**
- `SIGINT` — Ctrl+C
- `SIGTERM` — kill команда

### Фоновые задачи

Каждая фоновая задача теперь обрабатывает `CancelledError`:

```python
async def background_rewriter():
    while True:
        try:
            # работа...
        except asyncio.CancelledError:
            logger.info("🛑 Остановка rewriter...")
            break  # корректный выход
        except Exception as e:
            logger.error(f"❌ Ошибка: {e}")
        await asyncio.sleep(30)
```

### Отключение Telethon

```python
finally:
    if self.client.is_connected():
        await self.client.disconnect()
    logger.info("✅ Все задачи завершены")
```

---

## 🚀 Использование

### Остановка бота (Ctrl+C)

```bash
# Запуск
python main.py

# Остановка
^C  # нажми Ctrl+C

# Ожидаемый вывод:
# ⚠️  Получен сигнал SIGINT, завершаем работу...
# 👋 Останавливаем бота...
# ...
# ✅ Бот корректно остановлен
```

### Остановка через systemd

```bash
sudo systemctl stop telegram_bot

# Логи покажут:
# ⚠️  Получен сигнал SIGTERM, завершаем работу...
```

### Остановка через kill

```bash
# Найди PID процесса
ps aux | grep main.py

# Отправь SIGTERM
kill <PID>

# Или принудительно (не рекомендуется)
kill -9 <PID>  # SIGKILL - мгновенное завершение без очистки
```

---

## 🧪 Тестирование

### Тест 1: Ctrl+C во время работы

```bash
# 1. Запусти бота
python main.py

# 2. Подожди запуска фоновых задач
# Увидишь: 🚀 Бот запущен и слушает каналы...

# 3. Нажми Ctrl+C
^C

# 4. Проверь логи
tail -10 logs/bot_work.log
```

**Ожидаемый результат:**
```
⚠️  Получен сигнал SIGINT, завершаем работу...
👋 Останавливаем бота...
🛑 Остановка rewriter...
🛑 Остановка awaiting_closer...
🛑 Остановка post_builder...
🛑 Остановка publisher...
✅ Все задачи завершены
✅ Бот корректно остановлен
🛑 Завершение работы бота
```

### Тест 2: Ctrl+C во время рерайта

```bash
# 1. Запусти бота
python main.py

# 2. Отправь сообщение в источник
# Начнётся рерайт (займёт ~10 секунд)

# 3. Нажми Ctrl+C во время рерайта
^C

# 4. Проверь: задача должна завершиться корректно
```

---

## 🔍 Отладка

### Проблема: Бот не останавливается

**Возможные причины:**
1. Зависла задача в бесконечном цикле
2. Заблокирована база данных
3. Telethon не отключается

**Решение:**
```bash
# Принудительная остановка
kill -9 <PID>

# Проверь логи
tail -50 logs/bot_work.log

# Проверь PostgreSQL соединения
psql -U botuser -d telegram_bot -c "SELECT * FROM pg_stat_activity;"
```

### Проблема: Traceback при остановке

**Если всё ещё видишь traceback:**

1. Проверь версию Python:
```bash
python --version  # должно быть >= 3.8
```

2. Проверь, что используешь обновлённые файлы:
```bash
grep "signal_handler" main.py
# Должна быть функция signal_handler
```

---

## 📋 Checklist обновления

- [ ] Обновлён `main.py` с signal handler
- [ ] Обновлён `bot_logic.py` с CancelledError
- [ ] Перезапущен бот
- [ ] Протестирован Ctrl+C
- [ ] Логи показывают корректное завершение

---

## 🎯 Результат

**До:**
```
^C
Traceback...
KeyboardInterrupt
```

**После:**
```
^C
⚠️  Получен сигнал SIGINT, завершаем работу...
👋 Останавливаем бота...
✅ Бот корректно остановлен
```

Чистый, профессиональный выход! ✨
